apiVersion: v1
kind: Service
metadata:
  name: {{ include "testapp1.fullname" . }}
  labels:
    {{- include "testapp1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testapp1.selectorLabels" . | nindent 4 }}
